# Historical ablation reproduction environment.
#
# Install from the repository root:
#   python -m pip install -r requirements-historical.txt

# Research/runtime packages needed by the Section 6 historical pipeline.
# This intentionally excludes trading-terminal integrations that are not
# needed for the ablation reproduction.
numpy==1.26.4
pandas==2.2.3
optuna==4.7.0
numba==0.63.1
scipy>=1.11
pyarrow>=15.0
